# Simple HTTP server - no external dependencies needed
# Uses Python's built-in http.server module
# 
# If you want to add Playwright later, install with:
# pip install playwright
# playwright install chromium 